# v0.1.7 Release Notes

Rebuilt `aosp_cf_arm64_only_phone_qemu` outputs after baking
`ro.surface_flinger.max_frame_buffer_acquired_buffers=3` into the QEMU product.

## What Changed

- Added the QEMU-specific SurfaceFlinger default-property override
  `ro.surface_flinger.max_frame_buffer_acquired_buffers=3`.
- Kept the change in the tracked patch snapshot so the ignored `src/` checkout
  and the repo's recorded source delta stay aligned.
- Updated the QEMU graphics contract, validation checklist, and boot notes to
  describe the new baked runtime expectation.

## Validation

- `m -j$(nproc)` completed successfully for
  `aosp_cf_arm64_only_phone_qemu` on `2026-03-23`.
- Confirmed the generated output contains
  `ro.surface_flinger.max_frame_buffer_acquired_buffers=3` in
  `out/target/product/vsoc_arm64_only/vendor/build.prop`.
- `./scripts/prepare_plain_qemu.sh` completed successfully against the rebuilt
  image set.
- `DATE=2026-03-23 TAG=v0.1.7 ./scripts/make_release_bundle.sh` completed
  successfully and generated the release archive, manifest, checksum file, and
  release notes asset.

## What Is Included

- `aosp_cf_arm64_only_phone_qemu-out-2026-03-23.tar.zst`
- matching manifest and SHA-256 files
- generated release notes under `release-assets/v0.1.7-notes.md`

## How To Use It

1. Extract the archive under the repository root.
2. Place the included image files under `src/out/target/product/vsoc_arm64_only`.
3. Run `./scripts/prepare_plain_qemu.sh`.
4. Follow `docs/BOOT.md` for the plain-QEMU launch flow.

## Release Asset Paths

- `release-assets/aosp_cf_arm64_only_phone_qemu-out-2026-03-23.tar.zst`
- `release-assets/aosp_cf_arm64_only_phone_qemu-out-2026-03-23.manifest.txt`
- `release-assets/aosp_cf_arm64_only_phone_qemu-out-2026-03-23.sha256`
- `release-assets/v0.1.7-notes.md`

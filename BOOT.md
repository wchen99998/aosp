# Plain-QEMU Boot Notes

Canonical boot and validation instructions live in [`docs/BOOT.md`](docs/BOOT.md).

Use that file for:

- preparing a plain-QEMU bundle from a local build or `release-assets/`
- the validated no-GPU host launch recipe
- graphics-contract property checks
- first-boot and `adb` verification steps
